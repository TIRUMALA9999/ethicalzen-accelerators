# ACVPS Gateway - Local Development Environment Variables
#
# Setup Instructions:
# 1. Copy this file to .env.local
# 2. Get your gateway API key and tenant ID from the portal:
#    curl -X POST https://ethicalzen-backend-400782183161.us-central1.run.app/api/gateway/register \
#      -H "Authorization: Bearer YOUR_JWT_TOKEN" \
#      -H "Content-Type: application/json" \
#      -d '{"gateway_name": "my-local-gateway"}'
# 3. Fill in the values below
# 4. Run: docker-compose -f docker-compose-local.yaml --env-file .env.local up

# REQUIRED: Gateway Authentication
GATEWAY_API_KEY=gw_your_api_key_here
GATEWAY_TENANT_ID=tenant_your_tenant_id_here

# REQUIRED: Control Plane (Backend API)
CONTROL_PLANE_URL=https://ethicalzen-backend-400782183161.us-central1.run.app

# OPTIONAL: Your local backend service URL
# Use host.docker.internal to access services running on your host machine
BACKEND_URL=http://host.docker.internal:9001

# OPTIONAL: Redis for local caching (if using)
# REDIS_HOST=redis
# REDIS_PORT=6379
# REDIS_PASSWORD=

# OPTIONAL: Logging
LOG_LEVEL=debug
LOG_FORMAT=text

