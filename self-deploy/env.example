# =============================================================================
# AI Guardrails - Configuration
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp env.example .env

# Required: Your EthicalZen tenant API key
# Get from: https://ethicalzen.ai/dashboard → Settings → API Keys
GUARDRAIL_API_KEY=sk-your-tenant-key-here

# Optional: Custom ports (defaults shown)
GATEWAY_PORT=8080
METRICS_PORT=9090

# Optional: Override cloud-configured LLM keys
# Only needed if you want to use local LLM keys instead of cloud-configured ones
# LLM_API_KEY=sk-your-groq-or-openai-key
# LLM_PROVIDER=groq

# Advanced: Backend URL (normally don't change this)
# BACKEND_URL=https://ethicalzen-backend-400782183161.us-central1.run.app

